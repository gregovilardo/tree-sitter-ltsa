; Indentation for LTSA

[
  (process_definition)
  (composite_definition)
  (property_definition)
  (choice_process)
  (parallel_composition)
  (set_expr)
] @indent.begin

[
  ")"
  "]"
  "}"
  "."
] @indent.end

[
  ")"
  "]"
  "}"
] @indent.branch
